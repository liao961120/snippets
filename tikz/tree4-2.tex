\documentclass[preview,border=0]{standalone}
\usepackage{fontspec}
\usepackage{xeCJK}
\usepackage[paperheight=20cm,paperwidth=16cm]{geometry}
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning,shapes.multipart}
\setCJKmainfont{微軟正黑體}

\begin{document}

% \begin{tikzpicture}[
%   level distance=1.5cm,
%   sibling distance=2.5cm,
%   every node/.style={font=\footnotesize},
%   edge from parent/.style={draw, -{Latex[length=3mm]}, thick, > = stealth}, % Arrow style
%   level 1/.style={sibling distance=3.5cm},
%   level 2/.style={sibling distance=3cm},
%   level 3/.style={sibling distance=2cm}
% ]

\begin{tikzpicture}[
    thick, % line style
    > = stealth, % arrow head style
    edge from parent/.style={draw, -{Latex[length=3mm]}},
    every text node part/.style={align=center},
    level distance=2cm,
    U/.style={circle, draw=white, inner sep=-2pt, outer sep=-1pt, minimum size=6mm},
    % level 2/.style ={level distance=3cm},
  level 1/.style={sibling distance=4.1cm},
  level 2/.style={sibling distance=2cm,level distance=2.6cm},
  level 3/.style={sibling distance=1.5cm,level distance=2.2cm}
]

% Root node
\node (R) {$\textrm{孩子}_i$}
  child { node[U]{聽常 \\ ($z = 0$)}
    child { node[U]{未成熟 \\ ($k = 0$)}  edge from parent[->] 
        child{ node(A){是} edge from parent[->] }
    }
    child { node[U]{已成熟 \\ ($k = 1$)}  edge from parent[->] 
        child{ node(B){否} edge from parent[->] }
    }
    edge from parent[->]
  }
  child { node[U]{聽損 \\ ($z = 1$)}
    child { node[U]{未成熟 \\ ($k = 0$)}  edge from parent[->] 
        child{ node(C){是} edge from parent[->] }
    }
    child { node[U]{已成熟 \\ ($k = 1$)}  edge from parent[->] 
        child{ node(D){是} edge from parent[->] }
    }
    edge from parent[->] 
  };

  	% Nodes and their relative positions
    \node[U] (a) [below = .05 of A] {$1$};
    \node[U] (aa) [below = .05 of a] {$1 - \gamma_j$};

    \node[U] (b) [below = .05 of B] {$0$};
    \node[U] (bb) [below = .05 of b] {$G_j$};

    \node[U] (c) [below = .05 of C] {$1$};
    \node[U] (cc) [below = .05 of c] {$1 - \gamma_j$};

    \node[U] (d) [below = .05 of D] {$1$};
    \node[U] (dd) [below = .05 of d] {$1 - S_j$};


    \node[] (T1) [left = .7 of a]   {\footnotesize 答「是」的機率 \scriptsize (理想)};
    \node[] (T2) [below = .07 of T1] {\footnotesize 答「是」的機率 \scriptsize (現實)};


\end{tikzpicture}

\end{document}
